const Button = () => {
  return (
    <button
      type="button"
      style={{
        fontSize: 30,
        marginTop: 20,
        padding: 10,
        color: '#010101',
        border: '3px solid #FF0000',
      }}
    >
      collapse lines
    </button>
  );
};
export default Button;
